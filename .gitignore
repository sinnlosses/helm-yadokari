# dependencies
node_modules/

# build output
dist/

# pnpm cache (CI store)
.pnpm-store/

# environment variables (contains secrets)
.env

# TypeScript declaration files generated outside src/ (stale build artifacts)
*.d.ts
*.d.ts.map

# macOS
.DS_Store

.idea/

test-results.xml

# vitest coverage output
coverage/

# テストが cwd() 内に作る一時ディレクトリ（config.test.ts / env.test.ts）
test-tmp-*/

# バッチ実行レポート（REPORT_OUTPUT_PATH の既定の出力先。CI では artifacts として回収する）
report/
