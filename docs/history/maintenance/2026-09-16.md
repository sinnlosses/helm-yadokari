# ドキュメントの棚卸し（2026-09-16）

すべて対応済み（16件）。未対応は0件。

## 検査番号ごとの指摘件数（実行前 → 実行後）

| #   | 検査                         | 群   | 前 → 後 |
| --- | ---------------------------- | ---- | ------- |
| 1   | 索引の取りこぼし             | 確定 | 0 → 0   |
| 2   | 索引のリンク切れ             | 確定 | 0 → 0   |
| 3   | 先回りの空索引               | 確定 | 0 → 0   |
| 4   | 相対リンク切れ               | 確定 | 1 → 0   |
| 5   | タスク運用節の形             | 確定 | 0 → 0   |
| 6   | 実在しないパスの言及         | 候補 | 9 → 1   |
| 7   | 置き場の逸脱                 | 候補 | 1 → 1   |
| 8   | 正典の二重化                 | 候補 | 5 → 5   |
| 9   | 索引の無い大きなドキュメント | 候補 | 0 → 0   |
| 10  | スキル参照の不一致           | 候補 | 0 → 0   |

実行後に残っている7件は、すべて下の「そのままが正しかったもの」。新しい指摘は増えていない。

## 直したもの（6箇所）

**アーカイブの改名に追随した（5箇所）。** `docs/history/` のファイルは
`tasks-archive.md` → `tasks.md`、`progress-archive.md` → `progress.md` に改名済みで、
実体もその名前で存在する。古い名前を指していた記述は今の姿の説明としてズレていた。

| ファイル                  | 変更                                 |
| ------------------------- | ------------------------------------ |
| `docs/architecture.md:13` | 表のリンクを `./history/tasks.md` へ |
| `docs/workflow.md:26`     | `docs/history/tasks.md` へ           |
| `docs/workflow.md:32`     | `docs/history/tasks.md` へ           |
| `CLAUDE.md:193`           | `tasks.md` / `progress.md` へ        |

**廃止済みの `develop/workflow.json` の節を参照に置き換えた（1箇所）。**
`docs/workflow.md` の「## このプロジェクトの値（`develop/workflow.json`）」が、廃止された
設定ファイルとその値の表を現行として説明していた。検証コマンドと整形コマンドの正典は
`CLAUDE.md`「## タスク運用」節に移っているので、見出しを「## このプロジェクトの値」に変え、
中身を正典への参照1行にした。ユーザーの判断を得て実施。

## 移したもの（1ファイル・参照10箇所）

`docs/requirements-grilling.md` → **`docs/history/requirements-grilling.md`**。

要件検討時のQ&Aログで、`docs/history/` 配下と同じく当時の記述をそのまま残すもの。
`docs/` 直下にある限り棚卸しのたびに検査対象になり、中の旧パス（`src/lib/tag.ts` など3件）を
毎回「昔の話」と判定し直すことになっていた。**例外規則を足すのではなく、実体を正しい場所へ
動かす**判断（ユーザーの指示）。

追随させた参照:

| ファイル                                | 箇所 |
| --------------------------------------- | ---- |
| `CLAUDE.md`                             | 2    |
| `docs/requirements.md`                  | 2    |
| `docs/architecture.md`                  | 2    |
| `docs/workflow.md`                      | 1    |
| `docs/glossary.md`                      | 1    |
| `.claude/skills/maintain-docs/SKILL.md` | 3    |

`.claude/skills/maintain-docs/SKILL.md` は、このファイルを名指しで対象外にする規則を
3箇所（対象の記述・除外理由の箇条書き・`targets()` の `case` 文）に持っていた。
`docs/history/` 配下は元から対象外なので**規則ごと不要になった**ため削除し、対象ファイル数の
記述も8→7に直した。`docs/` の外だがこのスキルの範囲外に置くと壊れた参照が残るため追随させた。

`docs/history/` 配下に残る言及（`progress.md`・`tasks.md`・`direction.md` の多数）は
**当時の記録なので書き換えていない。**

## そのままが正しかったもの（7件・対応済み）

検査が挙げたが、中身を読んで「直さないのが正しい」と判断したもの。**未対応ではない。**

| 指摘                                                         | 判断                                                                                          |
| ------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| `docs/architecture.md` の `scripts/lint/verify-config/*` 2件 | 昔の話。935〜946行は `remote-existence/` へ移した記録そのもの                                 |
| `requirements-grilling.md` の `src/lib/*.ts` 3件             | 昔の話。Q&Aログなので `docs/history/` へ移した（下の「移したもの」）                          |
| 検査7 `architecture.md` に提案の記述                         | 正当。冒頭に「なぜその案を採らなかったのかだけを書く」とあり、経緯を正典に置く運用            |
| 見出し「CI/CD」が README.md と CLAUDE.md に重複              | 正当。CLAUDE.md 側が「説明は README.md「CI/CD」が正典」と明記＝参照                           |
| 見出し「エラーハンドリング」の重複                           | 正当。`docs/coding-standards.md` が「README 参照」と書いている                                |
| 見出し「タグ形式」の重複                                     | 正当。glossary は用語定義、README は仕様説明で役割が違う                                      |
| 見出し「環境変数」の重複                                     | 正当。README は利用者向けの変数一覧、coding-standards は `env.ts` 経由で読む規約              |
| `pnpm check` が CLAUDE.md の複数箇所                         | 正当。L35 はコマンド一覧、L78 は念押し、L158 はスキルが読む契約で役割が違う（ユーザーの判断） |

## 正典の寄せ替え

`docs/workflow.md` の「このプロジェクトの値」を、`CLAUDE.md`「## タスク運用」節への参照にした。
値を持つのは CLAUDE.md の1か所だけになった。

## 検証コマンド

`pnpm check` 通過（46ファイル・557テスト）。

途中1回、`docs/architecture.md` で `format:check` が落ちた。表のリンク文字列が短くなって
桁が揃わなくなったためで、`pnpm format` を通してから再実行した。
**ドキュメントだけの変更でも整形コマンドが要る**例。

## コミット

していない。作業ツリーに置いたまま。
